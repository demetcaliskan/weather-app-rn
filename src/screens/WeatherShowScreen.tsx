import { StyleSheet, Text } from 'react-native'
import ScreenLayout from '../components/ScreenLayout'

const WeatherShowScreen = () => {
  return (
    <ScreenLayout>
      <Text>Coming soon...</Text>
    </ScreenLayout>
  )
}

const styles = StyleSheet.create({})

export default WeatherShowScreen
